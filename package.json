{
  "type":"module",

  "scripts": {
    "dev": "npx tailwindcss -i input.css -o public/main.css -w",
    "build": "NODE_ENV=production npx tailwindcss -i input.css -o public/main.css -m"
  },
  "devDependencies": {
    "@prettier/plugin-ruby": "^3.2.2",
    "@tailwindcss/typography": "^0.5.9",
    "tailwindcss": "^3.3.1"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "jsdom": "^24.0.0",
    "node-fetch": "^3.3.2"
  }
}
